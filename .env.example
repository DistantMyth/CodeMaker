# ══════════════════════════════════════════════════════════════════
# CodeMaker Configuration
# Copy this file to .env and fill in your values
# ══════════════════════════════════════════════════════════════════

# ─── System Prompt ───────────────────────────────────────────────
# Instruction sent to the AI alongside the screenshot
SYSTEM_PROMPT=Solve this in c and have no comments at all.

# ─── Trigger Sequence ────────────────────────────────────────────
# Comma-separated key names. Default: triple-tab then triple-backspace
TRIGGER_SEQUENCE=tab,tab,tab,backspace,backspace,backspace

# ─── Screenshot ──────────────────────────────────────────────────
# Tool: grim, gnome-screenshot, spectacle, pillow, or auto
SCREENSHOT_TOOL=auto

# ─── Emergency Kill Combo ────────────────────────────────────────
KILL_COMBO=ctrl+shift+escape

# ─── Keyboard Device (Linux only) ────────────────────────────────
# Leave empty for auto-detect. Set to /dev/input/eventX if needed.
KEYBOARD_DEVICE=


# ══════════════════════════════════════════════════════════════════
# AI PROVIDERS — set up to 5 API providers + 1 local model
# Providers are tried in PROVIDER_PRIORITY order.
# Leave unused slots empty/commented out.
# ══════════════════════════════════════════════════════════════════

# ─── Priority Order ──────────────────────────────────────────────
# Comma-separated: 1-5 for API slots, "local" for Ollama
# First in list = highest priority, others are fallbacks
PROVIDER_PRIORITY=1,2,3,4,5,local


# ─── Provider 1 ──────────────────────────────────────────────────
# TYPE: gemini, groq, openrouter, mistral, together, github, openai
PROVIDER_1_TYPE=gemini
PROVIDER_1_KEY=
# Available Gemini models:
#   gemini-2.0-flash         → Fast, good for most code tasks (default)
#   gemini-2.0-flash-lite    → Fastest, lowest cost
#   gemini-2.5-flash         → Next-gen flash with built-in thinking
#   gemini-2.5-pro           → Most capable (limited free tier)
PROVIDER_1_MODEL=gemini-2.0-flash


# ─── Provider 2 ──────────────────────────────────────────────────
# Groq — extremely fast inference, generous free tier
# Get a free API key at: https://console.groq.com
PROVIDER_2_TYPE=groq
PROVIDER_2_KEY=
# Available Groq vision models:
#   meta-llama/llama-4-scout-17b-16e-instruct       → Best free vision model (recommended)
#   meta-llama/llama-4-maverick-17b-128e-instruct   → Larger, more capable
PROVIDER_2_MODEL=meta-llama/llama-4-scout-17b-16e-instruct


# ─── Provider 3 ──────────────────────────────────────────────────
# OpenRouter — aggregator, routes to cheapest provider
# Get free credit at: https://openrouter.ai
PROVIDER_3_TYPE=openrouter
PROVIDER_3_KEY=
# Available OpenRouter vision models:
#   google/gemini-2.0-flash-exp:free  → Free Gemini Flash
#   meta-llama/llama-4-scout:free     → Free Llama 4 Scout
#   qwen/qwen-2.5-vl-72b-instruct    → High quality vision
PROVIDER_3_MODEL=google/gemini-2.0-flash-exp:free


# ─── Provider 4 ──────────────────────────────────────────────────
# Mistral — free tier available
# Get a key at: https://console.mistral.ai
PROVIDER_4_TYPE=mistral
PROVIDER_4_KEY=
# Available Mistral vision models:
#   mistral-small-latest      → Fast, affordable
#   pixtral-large-latest      → Best Mistral vision model
PROVIDER_4_MODEL=pixtral-large-latest


# ─── Provider 5 ──────────────────────────────────────────────────
# Together AI — $1 free credit on signup
# Get a key at: https://www.together.ai
PROVIDER_5_TYPE=together
PROVIDER_5_KEY=
# Available Together vision models:
#   meta-llama/Llama-4-Scout-17B-16E-Instruct  → Fast vision
#   Qwen/Qwen2.5-VL-72B-Instruct               → Best quality
PROVIDER_5_MODEL=meta-llama/Llama-4-Scout-17B-16E-Instruct


# ─── Custom OpenAI-compatible endpoint ───────────────────────────
# Uncomment and use any slot (1-5) for a custom endpoint:
# PROVIDER_5_TYPE=openai
# PROVIDER_5_KEY=your_key
# PROVIDER_5_MODEL=your-model-name
# PROVIDER_5_BASE_URL=https://your-api.example.com/v1

# ─── GitHub Models (free with GitHub account) ────────────────────
# Uncomment and use any slot. Get a token at:
# https://github.com/marketplace/models
# PROVIDER_5_TYPE=github
# PROVIDER_5_KEY=your_github_token
# PROVIDER_5_MODEL=gpt-4o-mini
# PROVIDER_5_BASE_URL=https://models.inference.ai.azure.com


# ══════════════════════════════════════════════════════════════════
# LOCAL MODEL (Ollama) — runs on your GPU, no API key needed
# Install Ollama: https://ollama.com
# The model will be auto-downloaded on first use.
# ══════════════════════════════════════════════════════════════════

# Available local vision models (sorted by VRAM usage):
#   moondream:latest          → ~1.5 GB VRAM, basic vision
#   qwen2.5vl:3b             → ~2.5 GB VRAM, good vision + code (recommended for 6GB)
#   qwen2.5vl:7b             → ~5.0 GB VRAM, better quality (tight on 6GB)
#   llava:7b                 → ~5.0 GB VRAM, decent vision
#   llama3.2-vision:11b      → ~8.0 GB VRAM, excellent (needs 8GB+)
LOCAL_MODEL=
OLLAMA_URL=http://localhost:11434
